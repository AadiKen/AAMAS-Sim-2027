# Source documents

`bcod_sim_implementation_spec.md` is the implementation contract. The frozen
architecture document and `bcod_sim_invariants.yaml` follow it in authority.
The migration matrix records the disposition of legacy components. The Word
version of the specification is retained as supplied source material.

These files are snapshots, not generated from production code. Changes to them
require an explicit design decision.
